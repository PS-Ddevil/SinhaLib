export interface Book{
    id: string;
    name: string;
    author: string;
    genre: string;
}

export interface BookDetail{
    id: string;
    summary: string;
    no_pg: number;
    img: string;
}